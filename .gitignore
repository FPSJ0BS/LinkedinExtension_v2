node_modules/

# Derived build output. Regenerated by `npm run build` from tracked sources.
# The v3.4.0 build is archived in the baseline commit 5664fba if it is ever
# needed again.
dist/
.build/

# Installer archives. Derived from dist/ by `npm run package`, and rebuilt from
# tracked sources at any time, so the binary is never committed.
releases/
